name: C/C++ CI

on:
  push:
    branches: [ "13" ]
  pull_request:
    branches: [ "13" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: chmod +x build.sh && ./build.sh
